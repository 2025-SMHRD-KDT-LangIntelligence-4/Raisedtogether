<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.smhrd.web.mapper.LoginMapper">

	<select id="GoLoginParent" resultType="int" parameterType="com.smhrd.web.entity.Parent">
		select count(*)
		from t_parent
		where parent_id = #{parentId} and parent_pw = #{parentPw};
	</select>

	<select id="GoLoginSitter" resultType="int" parameterType="com.smhrd.web.entity.Sitter">
		select count(*)
		from t_sitter
		where sitter_id = #{sitterId} and sitter_pw = #{sitterPw};
	</select>
</mapper>